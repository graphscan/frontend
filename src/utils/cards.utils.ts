import { unixTimeToDateString } from "../utils/date.utils";

export const formatCardDate = unixTimeToDateString("MMMM dd, yyyy");

export const formatProfileCardDate = unixTimeToDateString("MMM dd, yyyy");
